/**Question # 4 */
// Famous Quote: Find a quote from a famous person you admire. Print the quote and the name of its author.
// Your output should look something like the following, including the quotation marks:

let quote = `Complain not to your fellow creatures against the Creator, and complain not to the Creator against hour fellow creatures. You will attain to peace.`;
let author = `Wasif Ali Wasif R.A`;
let message = `${author} once Said, "${quote}"`;
console.log(message);
